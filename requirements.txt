# Installing from this file does two things:
#   1. installs this project itself (the "-e ." line, an "editable" install),
#      which is what makes `python -m podcast_transcriber ...` work; and
#   2. pulls in the third-party libraries it needs (declared in pyproject.toml).
#
#   pip install -r requirements.txt
#
# We keep the dependency list itself in pyproject.toml so there's a single
# source of truth; this file just points at it.

-e .
